import Vue from 'vue';

new Vue({
    el: '#root',
    template: `<div></div>`
});

/**
 * v-bind    简写  :
 * v-on    简写  @
 * v-text
 * v-html
 * v-show
 * v-hide
 * v-if
 * v-else-if
 * v-else
 * v-for
 * v-model
 * v-pre
 * v-cloak(基本上用不到)
 * v-once
 */
